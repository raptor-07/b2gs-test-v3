version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm cache clean --force
        - npm cache verify
        - npm install 
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
