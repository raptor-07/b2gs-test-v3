import { Lexend, Aleo } from 'next/font/google'

export const lexend = Lexend({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-lexend'
})

export const aleo = Aleo({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-aleo'
})
